import { StyleSheet } from "react-native";
import { font } from "@product/theme";
export const styles = StyleSheet.create({
  container: {
    margin: 10
  },
  title: {
    marginTop: 10,
    marginBottom: 10,
    fontSize: font.textLarge
  },
  selectLocationBtn: {
    marginTop: 10,
    marginBottom: 10
  }
});
